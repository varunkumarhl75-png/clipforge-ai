"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
const API=process.env.NEXT_PUBLIC_API_URL;
type Analytics={total_projects:number;completed:number;processing:number;failed:number;total_duration:number;shorts_generated:number;transcripts:number;captions:number;edits:number};
export default function Analytics(){const [data,setData]=useState<Analytics|null>(null);const [error,setError]=useState("");useEffect(()=>{fetch(`${API}/api/analytics`).then(r=>r.ok?r.json():Promise.reject(new Error("Could not load analytics"))).then(setData).catch(e=>setError(e.message));},[]);return <main className="min-h-screen p-6 sm:p-10"><div className="mx-auto max-w-5xl"><Link href="/" className="text-sm text-slate-500 hover:text-violet-700">← Workspace</Link><p className="luxury-kicker mt-8 text-xs">Production overview</p><h1 className="mt-2 text-3xl font-semibold">Analytics</h1><p className="mt-2 text-slate-500">Your local production activity, calculated from stored records.</p>{error&&<p className="mt-6 text-red-600">Something went wrong loading analytics.</p>}{data&&<><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Projects",data.total_projects],["Completed",data.completed],["Processing",data.processing],["Failed",data.failed],["Duration (sec)",data.total_duration],["Shorts",data.shorts_generated],["Transcripts",data.transcripts],["Captions",data.captions],["Edits",data.edits]].map(([label,value])=><div key={label as string} className="rounded-2xl border border-stone-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">{label}</p><p className="mt-3 text-2xl font-semibold text-slate-900">{value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-amber-200 bg-amber-50 p-5 text-sm text-amber-800">YouTube analytics are not connected. Views, likes, retention, CTR, and watch time are not fabricated.</div></>}</div></main>}
